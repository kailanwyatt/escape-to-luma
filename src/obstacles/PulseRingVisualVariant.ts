/**
 * Pulse ring presentation switch.
 * Collision always uses PulseRingState — only meshes change.
 */
export type PulseRingVisualVariant = 'cinematic' | 'legacy';

/** Flip this to revert while curating. */
export const PULSE_RING_VISUAL_VARIANT: PulseRingVisualVariant = 'cinematic';
