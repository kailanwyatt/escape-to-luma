/**
 * Rolling aperture presentation switch.
 * Collision always uses ExtendedLibraryState — only meshes change.
 */
export type RollingApertureVisualVariant = 'cinematic' | 'legacy';

/** Flip this to revert while curating. */
export const ROLLING_APERTURE_VISUAL_VARIANT: RollingApertureVisualVariant = 'cinematic';
